---
enable_postgis: False

# use ppa instead of distribution repository
postgres_version_dev: False
postgres_ppa: "deb http://apt.postgresql.org/pub/repos/apt/ {{ ansible_distribution_release }}-pgdg main"
postgres_ppa_key: "https://www.postgresql.org/media/keys/ACCC4CF8.asc"

postgres_version: "9.4"

postgres_host: 127.0.0.1
postgres_port: 5432
