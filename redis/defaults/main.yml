---
# use ppa instead of distribution repository
redis_version_latest: False
redis_ppa: "deb https://packages.redis.io/deb {{ ansible_distribution_release }} main"
redis_ppa_key: "https://packages.redis.io/gpg"

redis_host: 127.0.0.1
redis_port: 6379
